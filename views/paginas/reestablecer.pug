<!DOCTYPE html>
html(lang="en")
    head
        include ../partials/header
    body.login
        .contenedor-formulario
            h1= nombrePagina
            p Agrega tu e-mail para restablecer la contraseña

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
                                
            form.caja-login(action="/reestablecer" method="post")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Correo electrónico" value= email)
                .campo.enviar
                    input.boton(type="submit" value="Reestablecer password")
                .campo.acciones
                    a.enlace(onClick="location.href='/iniciar-sesion'") Ya lo recordé, volver a login