<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        style
            include ./email.css
    body
        h2 Confirma tu cuenta UpTask
        p Ya casi estás inscrito, solo presiona el siguiente botón.
        a(href=`${confirmarUrl}`) Confirmar!
        p Si no puedes accede a este enlace, visita #{confirmarUrl}
        p Si no solicitaste este e-mail, puedes ignorarlo.